---
status: open
priority: 30
kind: feature
tags:
    - find
---

# add property printing into the `find` command

for example:
```bash
tasker find .status eq open -p tags
```

will print the tasks which the TQL matches and their tags property
